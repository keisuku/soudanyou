# Homepage / LP Prompt

Improve soudanyou's homepage or LP-like sections.

Required flow:
1. Read `AGENTS.md` and `docs/design/`.
2. Update `PLAN.md`.
3. Keep important copy as HTML text, not baked into images.
4. Reuse existing components and Tailwind v4 patterns.
5. Run `npm run lint` and `npm run build` when feasible.
6. Inspect desktop and mobile browser output.
